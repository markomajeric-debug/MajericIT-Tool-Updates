# MajericIT-Tool-Updates

Public release channel for the self-update check in Majeric IT Deployment Tool.
Contains only `update-manifest.json` (read by the tool's `ToolUpdateCheckUrl`)
and signed MSI/EXE installers as GitHub Release assets. Source code lives in
a separate private repository.
